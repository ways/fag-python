# Fag-Python

This is a test-skill developed for demo purposes. For use in APM.

## Usage

In your python based project:

- Add an apm.yml file by running `apm init`
- Add this to the file:

```yaml
dependencies:
  apm:
    - git@gitlab.met.no:larsfp/apm-testskill.git
```

- Run `apm install`
